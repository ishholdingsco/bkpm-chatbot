// Reuse the Open Graph image for the Twitter/X card so there is a single source
// of truth (and a single rendered PNG to keep small). See app/opengraph-image.js.
export { default, alt, size, contentType } from "./opengraph-image";
